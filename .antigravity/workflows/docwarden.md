# doc-sync workflow

Trigger phrase: "run doc-sync" / "update the docs"

Step 1: Read `skills/doc-sync/SKILL.md` in this repository.
Step 2: Follow it exactly as written. It is the single source of truth —
do not duplicate or reinterpret its rules in this workflow file.
