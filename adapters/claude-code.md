# Claude Code adapter

No adapter file needed. Claude Code reads `SKILL.md` files directly.

Install globally (works in every repo):
    cp -r skills/docwarden ~/.claude/skills/docwarden

Or per-project only:
    cp -r skills/docwarden .claude/skills/docwarden

Trigger: type "update the docs" or "run docwarden" in chat.
