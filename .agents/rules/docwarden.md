# doc-sync (always-on rule variant)

This file does not contain its own rules. It is a pointer, kept in sync
with `skills/doc-sync/SKILL.md`, which is the single source of truth.

Do not edit the ruleset here. Edit `skills/doc-sync/SKILL.md` and re-run
the sync step in the project README so this file and every other adapter
stay identical.

---

TRIGGER: only act on this when the user explicitly asks to sync, update,
or refresh documentation (e.g. "update the docs", "run doc-sync"). This
is a manually-invoked task, not something to run on every turn.

FULL INSTRUCTIONS: read `skills/doc-sync/SKILL.md` in this repository
before acting, and follow it exactly.
